import React from 'react';

const Question3 = () => {
    const multiply = (a,b) => a * b;

    const result = multiply(5, 10);
  return (
    <div>
        <p>Result of multiplication: {result}</p>
    </div>
  )
}

export default Question3